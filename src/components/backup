import React from 'react';
import './MenuItem.css';

const MenuItem = (props) => {

	let itemDetails = { id: props.id, name: props.name, price: props.price, type: props.type };

	return <div className="item" onClick={props.addItem(itemDetails)}>{props.name} - ${props.price}</div>;

};

export default MenuItem;